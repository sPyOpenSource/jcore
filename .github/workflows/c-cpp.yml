name: C/C++ CI

on:
  push:
    branches: [ raspberrypi ]
  pull_request:
    branches: [ raspberrypi ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: make
